import { Button } from "@/components/ui/moving-border";
import Link from "next/link";

export default function NotFound() {
    return (
        <main className="dark:bg-black dark:text-white flex flex-col items-center justify-center h-screen w-full">
        </main>
    );
}